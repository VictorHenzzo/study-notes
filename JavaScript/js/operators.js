const number = 5;
const text = '5';

console.log(number==text);
console.log(number===text);

const minimalAge = 18;
const clientAge = 15;

console.log(clientAge >= minimalAge ? 'Can drive': 'Cannot drive!');
